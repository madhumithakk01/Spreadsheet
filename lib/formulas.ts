/**
 * Formula utilities (scaffolding).
 * =SUM, arithmetic, and other formula logic will be implemented here later.
 * Do not implement formula logic in this step.
 */

export type FormulaResult = string | number | null;

/**
 * Placeholder: will evaluate cell formulas (e.g. =SUM(A1:A10)) in a later step.
 */
export function evaluateFormula(_formula: string): FormulaResult {
  return null;
}
